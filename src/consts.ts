import { SIDE_WIDTH, HEADER_HEIGHT } from "./grid";

export const heightcss = `height:${HEADER_HEIGHT}px;max-height:${HEADER_HEIGHT}px;min-height:${HEADER_HEIGHT}px`;
export const widthcss = `width:${SIDE_WIDTH}px;`;
